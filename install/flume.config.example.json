{
  "secretsEngine": "openbao",
  "openbao": {
    "addr": "http://127.0.0.1:8200",
    "mount": "secret",
    "path": "flume",
    "tokenFile": "~/.config/flume/openbao.token"
  },
  "_comment": "Copy to flume.config.json at the repo root. Put your OpenBao token in tokenFile (chmod 600). Store ES_API_KEY, LLM_API_KEY, GH_TOKEN, and other runtime values in KV at secret/flume (see install/setup/sync-bootstrap-to-openbao.sh)."
}
