# Provisioning: copy to install/.elastic-admin.env (chmod 600, never commit).
# Flume reads this to create ES_API_KEY automatically; the end user never sees the password.
#
# ELASTIC_PASSWORD=your-generated-elastic-password
# ELASTIC_USERNAME=elastic
