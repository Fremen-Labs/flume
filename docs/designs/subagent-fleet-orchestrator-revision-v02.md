# Subagent Fleet Orchestrator Design - Revision v0.2 (Addressing 2026-06 Review)

**Date:** 2026-06-01 (post-tool verification)  
**Based on:** v0.1 design (582 lines, all citations tool-confirmed) + structured review (2 major + 3 minor + 4 nits)  
**Process:** Followed Flume reliable-go-systems SKILL (adapted for harness: reconciliation via scheduler/monitor + file, rich structured reasoning capture via todo/logs, OCC/shadow EnforceOrLog on state, bounded, explicit, idempotent, observability-first, no unbounded). Used todo_write for phases (read-all, runtime-section, verification-appendix, alt5-elevate, updates-to-prs-oq-checklist, final-write; merge:true after init). All harness claims (tool names, spawn params, persona prefix, manual ID in skills, compaction staleness, meta.json schema, Ctrl+T/Ctrl+;, no persona param support, scheduler_create/durable, 5 IDs, etc.) re-verified with grep/read_file/open on exact sources before asserting in appendix. Flume citations re-audited via grep on internal/worker/*.go (e.g. manager.go:170 cycle). No edits to other files; only design + summary. Bounded scope: addressed review feedback exactly, preserved strong parts (Flume mapping table, diagrams desc, API specs, concrete citations, PR structure, Key Decisions, data model).

## Per-Issue Status

**Major 1 (runtime mismatch):** Addressed. Added new early section "Runtime & Language Mapping for Harness" (right after Core Components in Proposed Design). Explicitly maps cycle/sweeper/claimer to harness primitives: `scheduler_create` (recurring for RunThrottled heartbeats + /loop), `monitor` (for event/file streams), background tasks + `get_command_or_subagent_output`/`spawn_subagent`, file polling via list_dir/read_file or scheduled recon (no goroutines/long-running ticker inside agent turn model). Rewrote pseudocode (now lang-agnostic + harness equiv using scheduler_create + monitor + todo_write for phases), architecture notes, Runner pseudocode, and PR1/overall to start with "PR0: 1-2 day runtime spike / prototype using *only* existing scheduler/monitor/spawn surfaces + file durable records (no assumption of in-process Go ticker or goroutines)". Notes "core changes (if any) are for the underlying ACP/harness runtime team (e.g. native TUI grouping, streaming bridge)". Aligned with existing `~/.grok/bundled/skills/fleet/SKILL.md:38` (harness native: "No long-running goroutine ticker inside agent turns. Use `scheduler_create` ... for the "cycle" and `RunThrottled`") and 20-background-tasks.md scheduler/monitor surfaces. (Verified via multiple read_file/grep on 20-*.md:146 scheduler_create params incl. durable; fleet/SKILL.md:38 quote.)

**Major 2 (verification):** Addressed. Added new "Verification Log / Appendix" section at end (after References, before Key Decisions in structure but post-References per flow). For *every* harness claim, lists *exact* source (file:line or tool output quote from read/grep calls). Resolved Open Question on tool registration: standardize on `spawn_subagent` (not "task") for new fleet_* tools (and all calls); docs call it "task tool" for UX. Re-audited the 5 subagent IDs narrative as "illustrative example drawn from observed parallel usage patterns in recent sessions and skills (exact IDs from design context; high call counts and babysitting observed)" + cites actual meta.json artifacts (e.g. .../019e8576-.../subagents/019e8677-614d-.../meta.json with "tool_calls":119, description matching "fix-install", "status":"completed", "duration_ms", plus siblings for the other 4 IDs with 88-101 calls). All claims double-checked with tools before final write. (See appendix for 20+ specific citations.)

**Issue 3 (PR plan/TUI/e2e):** Addressed (minor). Promoted minimal TUI grouping/read-only fleet list to PR1 (foundations + early TUI stub) + PR2. Added basic sweeper subset note for possible early in PR2 (manual fleet_sweep). Added explicit "e2e test task (replay the 5-delegation contract using new tools + assert no manual ID babysitting + durable recovery post-simulated compaction)" to PR4/5. Updated all PR titles/descriptions/ordering notes + added per-PR "risk if delayed" + "investigate pager vs core TUI ownership + minimal change spike" explicit in PR1. (Aligned with review suggestions.)

**Issue 4 (Alternatives/Key Decisions):** Addressed (minor). Elevated Alternative 5 ("Harness-native via extended scheduler + monitor + file-state + todo orchestration skill, no new core heartbeat") to full section (new #5 after 4) with detailed trade-offs (faster prototype leveraging existing /loop/scheduler surfaces + fleet/SKILL.md stub, full coexistence/min core change; vs less "first-class" native TUI/claim until pager/core extends). Re-visited Key Decision 7 (coexistence remains non-negotiable; alt5 makes "fleet as skill-orchestrated" viable primary path initially) and Risks "core complexity" (now qualified: "High if core loop chosen; mitigated/low if alt5 harness-native selected for v1 (as in current fleet/SKILL.md + 23- doc foundations)"). 

**Issue 5 (Open Questions + "directly implementable"):** Addressed (minor). Promoted top 4 OQs to "**Blocking**" with proposed answers (tool name: spawn_subagent convention; TUI ownership: pager+stub early, spike in PR1; durability scope: per-session default + scheduler durable opt-in; embed for dedup: vec0 exists per 13-memory.md, use norm+opt semantic). Updated Goals, closing sentence, and PR1 to "implementable after resolving blocking open questions and completing the PR0 runtime spike". Added new "Pre-Implementation Checklist" section (with spike, verify appendix, align fleet skill/23-doc, etc.).

**Issue 6 (5 IDs factual):** Addressed (nit). Rephrased narrative in Overview + Background + References + appendix to "illustrative example drawn from observed parallel contract-style usage patterns in recent sessions and skills (exact IDs from design context; high tool-call counts and manual ID tracking observed in skills and session artifacts)". Confirmed via grep/read on actual meta.json (e.g. 019e8677-614d-... "tool_calls":119, Elastro/LogLoom desc, completed status; 4 siblings with 88-101 calls; parent session 019e8576-...).

**Issue 7 (Security/ACP thin):** Addressed (nit). Added 1-2 sentences + table row in Security section: "New fleet tools registered with same permission_mode checks as spawn_subagent; delegation records respect parent session ACLs; no new YOLO surface. ACP schema updates (if any) limited to adding the 6 fleet_* entries mirroring spawn_subagent. See 22-permissions-and-safety.md (subagent control read-class) and 07-mcp-servers.md for MCP tool surface."

**Issue 8 (Observability unverified bridge):** Addressed (nit). Added verification note in Observability + Runtime section + appendix: "Assumption: harness subagent sessions emit structured reasoning deltas (or accessible via existing get_* + parse + monitor); if only full transcripts, bridge falls back to post-completion summary extraction from summary_file + last N turns (per persona contracts in implement/SKILL.md). Confirm in harness core or add minimal capture hook in PR5. (Wiring reuses scheduler/monitor + get_ per 20-background-tasks.md.)"

**Other:** Kept *all* strong parts (concrete Flume citations with file:func:line verified via grep, full mapping table, Mermaid descs, exact API specs with before/after, data model JSON, PR plan structure, Key Decisions 1-7, data model, rollout, risks, refs). Updated fleet/SKILL.md and 23- already reference v0.2/runtime spike/harness-native (tool-confirmed). Used rich reasoning in todos + this summary (per reliable-go + agent orchestration layer in SKILL). No unbounded work; all via tools + bounded reads/greps.

**Verification performed (tools only, no assumptions):** 30+ grep/read_file calls on: design+review (full), ~/.grok/skills/reliable-go-systems/SKILL.md (full), 16-subagents.md (full+targeted L40-70/198+), 20-background-tasks.md (full+targeted L146+), implement/SKILL.md (L1-100 + 55-60 + 400-420 + 404-419 + 646-650 + 45 reseed), pr-babysit/SKILL.md (L1-100 + 400-420 + 405-462 + 30/407/438/419 prefix + end fleet opt-in), fleet/SKILL.md (full), sessions meta.json examples (multiple, e.g. 019e8677-614d... for IDs + schema), workspace internal/worker/manager.go:170 etc, .grok/docs/23-fleet-orchestrator.md, 13-memory.md (vec0), 22-*, 08-*, 03-*, 07-*. All claims quoted verbatim in appendix. 5 IDs confirmed in live artifacts. Aligned with pr-babysit/implement manual patterns + fleet stub already using alt5/harness-native.

*This v0.2 is now ready for PR0 spike + implementation after blocking OQs.*

---

(End of revision summary. Full v0.2 design in sibling file with integrated changes + appendix + checklist.)